[build]
  command = "pnpm install --no-frozen-lockfile && pnpm --filter @workspace/yt-seo-helper run build"
  publish = "artifacts/yt-seo-helper/dist/public"
  functions  = "netlify/functions"

[build.environment]
  NODE_VERSION  = "20"
  PNPM_VERSION  = "10"
  BASE_PATH     = "/"
  # CI=false prevents pnpm from defaulting to --frozen-lockfile on its auto-install step
  CI            = "false"

# Proxy /api/trending → Netlify Function (no backend server needed)
[[redirects]]
  from   = "/api/trending"
  to     = "/.netlify/functions/trending"
  status = 200

# SPA fallback — all other paths serve index.html
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
